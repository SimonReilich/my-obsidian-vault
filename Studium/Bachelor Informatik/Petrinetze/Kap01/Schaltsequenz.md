---
lecture: "[[Petrinetze]]"
---
#BScInfo #Informatik #Petri
# Definition 
Sei $N = (S, T, F)$ ein [[Netz]] und sei $M$ eine [[Markierung]] von $N$. Eine endliche [[Folge]] $\sigma = t_1 ... t_n$ ist schaltbereit, wenn es [[Markierung|Markierungen]] $$

A finite sequence o = t1... tn is enabled at a marking M if there are mark-

write M M.

The empty sequence e is enabled at any marking and we have M M.

If M M' for some markings M, M' and some sequence o, then we write M→ M' and sav that M' is reachable from M.

M) denotes the set of markings that are reachable from M.

An infinite sequence o = t₁t2... is enabled at a marking if there are mark- ings M₁, M2,. such that M M₁12 M₂